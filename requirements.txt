# Python/FastAPI frontend — renders every browser-facing page and proxies
# everything else to the Go backend. See docs/FRONTEND.md.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1,<4.0
httpx>=0.27,<1.0
